{
  // 当前工作区默认编码：GBK（GB2312/GB18030 兼容）
  "files.encoding": "gbk",
  // 开启自动猜测编码（优先识别文件实际编码，减少乱码）
  "files.autoGuessEncoding": true,
  // 可选：仅对特定语言（如cpp、java）强制GBK，其他保持默认
  "[VBA]": {
    "files.encoding": "gbk"
  }
}