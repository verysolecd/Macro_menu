Sub CATMain()
    Set oPrd = CATIA.ActiveDocument.Product
    Dim refprd: Set refprd = oPrd.ReferenceProduct
    Dim oPrt: Set oPrt = refprd.Parent.Part
    Set Pubs = refprd.Publications
    
    
'    colls.SetDirect Mass, oref
'
    
    pubid = "pub1"
    Set Target = refprd.UserRefProperties.item("Mass")
    Set oref = refprd.CreateReferenceFromName(Target.Name)
    Dim oPub
    Set oPub = Pubs.Add(pubid)
    
    
    
    Pubs.SetDirect pubid, refObj
    End Sub

